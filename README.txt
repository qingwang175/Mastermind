@author Qing Wang (qw2328)
@author Katelyn Ge (kbg488)
EE422C Bonus Assignment

The package around both classes are named "mastermind".

The Driver.java file is mostly just the main() method that runs the game in a loop.

Game.java holds game-specific information and methods for sorting through the inputs.

We use JOPtionPane to make choices and inputs, outputs are given through the system console.
